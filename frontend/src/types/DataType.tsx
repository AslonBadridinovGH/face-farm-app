
export type UserDataType = {

    labels: number[],
    datasets: {
          label: string;
          data: number [];
          backgroundColor: string [];
          borderColor: string;
          borderWidth: number;
    }[];
}